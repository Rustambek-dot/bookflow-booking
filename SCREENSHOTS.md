# Screenshots Checklist — BookFlow

1. Landing (`/`) — hero + features (светлая тема)
2. Виджет `/book` — шаг выбора услуги
3. Виджет `/book` — сетка временных слотов
4. Виджет `/book` — экран подтверждения
5. Dashboard — расписание дня + график недели
6. Bookings — таблица с фильтрами и статусами
7. Services — карточки CRUD с формой добавления
8. Staff — карточки мастеров с графиками
9. Analytics — utilization + booking outcomes (pie)
10. Мобильный вид виджета (375px) — главный экран для клиента

Рекомендация: виджет снимать в светлой теме, дашборд — в обеих.
